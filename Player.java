public class Player implements IPlayer {
     /*
     public void move(Coordinates initialPosition, Coordinates finalPosition ){
          Figure figureToMove = Board.identifyFigure(initialPosition.getRow(),initialPosition.getColumn());

     }
     */
}
